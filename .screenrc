# long history
defscrollback 5000

# no splash screen (on by default on mac)
startup_message off

# no barking
vbell_msg ""

# Pretty status line
hardstatus alwayslastline '%{= k} %= %{= kw}%?%-Lw%?%{r}(%{W}%n* %f%t%?(%u)%?%{r})%{w}%?%+Lw%?%? %= %D %M %d %c '
